---
title: ""
pagetitle: "PCA — R Scripts"
output:
  html_document:
    self_contained: false
    toc: true
    toc_depth: 4
    toc_float:
      collapsed: true
      smooth_scroll: true
    code_folding: show
    highlight: tango
    df_print: paged
    css: assets/custom.css
    includes:
      after_body: assets/after_body.html
---

```{=html}
<style>
/* Custom function TOC — overrides rmarkdown floating TOC */
#TOC,
.tocify-wrapper,
div[data-spy="affix"] {
  display: none !important;
  width: 0 !important;
  margin: 0 !important;
  padding: 0 !important;
}
.fn-nav {
  position: fixed;
  top: 60px;
  left: 0;
  width: 205px;
  height: calc(100vh - 60px);
  overflow-y: auto;
  padding: 14px 10px 20px 14px;
  border-right: 1px solid #e8e8e8;
  background: #fafafa;
  font-size: 0.82em;
  z-index: 100;
}
.fn-nav .fn-nav-title {
  font-size: 0.7em;
  font-weight: 700;
  letter-spacing: .08em;
  text-transform: uppercase;
  color: #bbb;
  margin-bottom: 10px;
  padding-left: 4px;
}
.fn-nav ul {
  list-style: none;
  margin: 0;
  padding: 0;
}
.fn-nav li a {
  display: block;
  padding: 3px 6px;
  border-radius: 4px;
  color: #444;
  text-decoration: none;
  font-family: monospace;
  font-size: 0.95em;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.fn-nav li a:hover {
  background: #eef2f7;
  color: #2c3e50;
}
.fn-nav li a.active {
  background: #e3f0fb;
  color: #1a6faf;
  font-weight: 600;
}
/* push main content right to make room */
body {
  margin-left: 0 !important;
  padding-left: 0 !important;
}
.main-container {
  margin-left: 210px !important;
  max-width: calc(100vw - 230px) !important;
  width: calc(100vw - 230px) !important;
  padding-right: 30px !important;
}
/* kill rmarkdown's bootstrap TOC column entirely */
.col-xs-12.col-sm-4.col-md-3,
.tocify-wrapper,
div.tocify,
#TOC { display: none !important; width: 0 !important; padding: 0 !important; margin: 0 !important; }
/* expand content column to full width */
.col-xs-12.col-sm-8.col-md-9 {
  width: 100% !important;
  -webkit-flex: none !important;
  flex: none !important;
}
.row { margin-left: 0 !important; margin-right: 0 !important; }
</style>

<div class="fn-nav" id="fn-nav">
  <div class="fn-nav-title">Functions</div>
  <ul>
    <li><a href="#fn-make.pca">make.PCA</a></li>
    <li><a href="#fn-make.pca.groups">make.PCA.groups</a></li>
    <li><a href="#fn-pca-plot">PCA_plot</a></li>
    <li><a href="#fn-pca-plot-funspace">PCA_plot_funspace</a></li>
    <li><a href="#fn-make-pca-plot">make_pca_plot</a></li>
    <li><a href="#fn-make-pca-plot-34">make_pca_plot_34</a></li>
    <li><a href="#fn-format-correlation-table">format_correlation_table</a></li>
    <li><a href="#fn-run-procrustes">run_procrustes</a></li>
  </ul>
</div>

<script>
/* Highlight active function on scroll */
window.addEventListener("scroll", function() {
  var links = document.querySelectorAll(".fn-nav li a");
  var current = "";
  links.forEach(function(a) {
    var id = a.getAttribute("href").slice(1);
    var el = document.getElementById(id);
    if (el && el.getBoundingClientRect().top < 120) current = id;
  });
  links.forEach(function(a) {
    a.classList.toggle("active", a.getAttribute("href") === "#" + current);
  });
});
</script>

<style>
#pw-overlay {
  position: fixed; inset: 0; background: rgba(255,255,255,0.97);
  z-index: 9999; display: flex; align-items: center; justify-content: center;
}
.pw-box {
  background: #fff; border: 1px solid #dde3ea; border-radius: 12px;
  padding: 40px 48px; text-align: center;
  box-shadow: 0 4px 24px rgba(0,0,0,0.08); max-width: 400px; width: 90%;
}
.pw-box h2 { margin: 0 0 6px 0; font-size: 1.5em; color: #2c3e50; }
.pw-box p  { color: #666; font-size: 0.95em; margin: 0 0 22px 0; }
.pw-lock   { font-size: 3em; margin-bottom: 8px; }
.pw-row    { display: flex; gap: 8px; }
.pw-row input {
  flex:1; padding: 10px 14px; border: 1px solid #ccc;
  border-radius: 8px; font-size: 1em; outline: none;
}
.pw-row input:focus { border-color: #16a085; }
.pw-row button {
  padding: 10px 18px; background: #16a085; color: #fff;
  border: none; border-radius: 8px; font-size: 1em; cursor: pointer;
}
.pw-row button:hover { opacity: 0.85; }
.pw-err { color: #c0392b; font-size: 0.9em; margin: 8px 0 0 0; min-height: 20px; }
.back-link { margin: 20px 0 4px 0; font-size: 0.9em; }
.back-link a { color: #16a085; text-decoration: none; }
.back-link a:hover { text-decoration: underline; }

/* ── Function cards ────────────────────────────────── */
.page-intro {
  display:flex; align-items:center; gap:14px;
  padding-bottom:18px; border-bottom:1px solid #e5e5e5; margin-bottom:28px;
}
.page-intro-icon {
  width:48px; height:48px; border-radius:10px;
  display:flex; align-items:center; justify-content:center;
  font-size:1.5em; color:#fff; flex-shrink:0;
}
.page-intro h1 { font-size:1.6em; font-weight:bold; color:#2c3e50; margin:0 0 4px 0; }
.page-intro p  { font-size:0.92em; color:#777; margin:0; }

.fn-section-divider {
  display:flex; align-items:center; gap:10px; margin:28px 0 14px 0;
}
.fn-section-divider span {
  font-size:0.75em; font-weight:700; letter-spacing:.08em;
  text-transform:uppercase; color:#aaa; white-space:nowrap;
}
.fn-section-divider hr { flex:1; border:none; border-top:1px solid #e8e8e8; }

.fn-card {
  border:1px solid #dde3ea; border-radius:10px; overflow:hidden; margin-bottom:20px;
}
.fn-header {
  display:flex; align-items:flex-start; justify-content:space-between;
  padding:12px 16px; background:#f8f9fa; border-bottom:1px solid #eee; gap:12px;
}
.fn-name {
  font-family:monospace; font-size:0.97em; font-weight:700; color:#2c3e50;
  margin:0; padding:0; line-height:1.4;
}
.fn-desc { font-size:0.85em; color:#666; margin-top:3px; line-height:1.5; }
.fn-tags { display:flex; gap:6px; flex-wrap:wrap; margin-top:6px; }
.fn-tag {
  font-size:0.72em; padding:2px 8px; border-radius:20px; font-weight:600;
}
.tag-stat   { background:#e8f5e9; color:#1b6b2f; }
.tag-util   { background:#e3f2fd; color:#1557a0; }
.tag-viz    { background:#f3e5f5; color:#6a1b9a; }
.tag-fd     { background:#fff3e0; color:#b25000; }
.tag-data   { background:#fce4ec; color:#880e4f; }
.tag-ml     { background:#e8eaf6; color:#283593; }
.tag-morph  { background:#e0f2f1; color:#00574e; }

.fn-args {
  display:flex; gap:7px; flex-wrap:wrap; padding:8px 16px;
  background:#fff; border-bottom:1px solid #f0f0f0; align-items:center;
}
.fn-args-label { font-size:0.78em; font-weight:700; color:#999; }
.fn-arg {
  font-family:monospace; font-size:0.78em; padding:2px 8px;
  background:#f1f3f5; border:1px solid #e0e3e8; border-radius:5px; color:#555;
}

</style>

<div id="pw-overlay">
  <div class="pw-box">
    <div class="pw-lock">🔒</div>
    <h2>PCA</h2>
    <p>Please enter the access password.</p>
    <div class="pw-row">
      <input type="password" id="pw-input" placeholder="Password…"
             onkeydown="if(event.key==='Enter') checkPw()"/>
      <button onclick="checkPw()">Enter</button>
    </div>
    <p class="pw-err" id="pw-error"></p>
  </div>
</div>

<div class="back-link">← <a href="Rscripts.html">Back to R Scripts</a></div>

<script>
const CORRECT_HASH = "ac95cc34e3ff9d4e59fd3066f6feaee0d2f4f7f97d3865148bc5767834883ff5";
async function sha256(msg) {
  const buf = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(msg));
  return Array.from(new Uint8Array(buf)).map(b=>b.toString(16).padStart(2,"0")).join("");
}
async function checkPw() {
  const hash = await sha256(document.getElementById("pw-input").value);
  if (hash === CORRECT_HASH) {
    document.getElementById("pw-overlay").style.display = "none";
    sessionStorage.setItem("rscripts_unlocked", "1");
  } else {
    const err = document.getElementById("pw-error");
    err.textContent = "Incorrect password. Please try again.";
    document.getElementById("pw-input").value = "";
    document.getElementById("pw-input").focus();
    setTimeout(()=>{ err.textContent=""; }, 3000);
  }
}
if (sessionStorage.getItem("rscripts_unlocked") === "1")
  document.getElementById("pw-overlay").style.display = "none";
</script>
```


```{=html}
<div class="page-intro">
  <div class="page-intro-icon" style="background:#16a085;">🔵</div>
  <div>
    <h1>PCA</h1>
    <p>Principal component analyses, functional space biplots, density plots and Procrustes</p>
  </div>
</div>
```


```{=html}
<h3 class="fn-section-divider"><span>Compute PCA</span><hr></h3>
```

```{=html}
<div class="fn-card">
<div class="fn-header">
  <div>
    <h4 class="fn-name" id="fn-make.pca">make.PCA(TRAITS, dimensionsAux, savePCA)</h4>
    <div class="fn-desc">Run a PCA on a trait matrix and return axes scores, loadings and explained variance.</div>
    <div class="fn-tags"><span class="fn-tag tag-stat">statistics</span><span class="fn-tag tag-fd">functional diversity</span></div>
  </div>
</div>
<div class="fn-args"><span class="fn-args-label">Args:</span><span class="fn-arg">TRAITS — species × traits matrix</span><span class="fn-arg">dimensionsAux — number of axes to retain</span><span class="fn-arg">savePCA — file path to save the result</span></div>
```

```{r fn_pca_make_PCA_0, eval=FALSE}
make.PCA <- function(TRAITS, dimensionsAux, savePCA) {
  paranAux <- paran(TRAITS)
  PCA_TRAITS <- list()
  PCA_TRAITS$traits <- TRAITS
  PCA_TRAITS$PCA <- princomp(PCA_TRAITS$traits)
  PCA_TRAITS$dimensions <- dimensionsAux
  PCA_TRAITS$variance <- (summary(PCA_TRAITS$PCA)[1][[1]]^2)[1:dimensionsAux] /
    sum(summary(PCA_TRAITS$PCA)[1][[1]]^2)
  PCA_TRAITS$loadings <- PCA_TRAITS$PCA$loadings
  PCA_TRAITS$traitsUse <- data.frame(PCA_TRAITS$PCA$scores[, 1:PCA_TRAITS$dimensions])
  if (!is.na(savePCA)) {
    saveRDS(PCA_TRAITS, savePCA)
  }
  return(PCA_TRAITS)
}
```

```{=html}
</div>
```

```{=html}
<div class="fn-card">
<div class="fn-header">
  <div>
    <h4 class="fn-name" id="fn-make.pca.groups">make.PCA.groups(traitsImputed, traitsMissing, ...)</h4>
    <div class="fn-desc">PCA with separate point sets for imputed and missing-trait species.</div>
    <div class="fn-tags"><span class="fn-tag tag-stat">statistics</span><span class="fn-tag tag-fd">functional diversity</span></div>
  </div>
</div>
<div class="fn-args"><span class="fn-args-label">Args:</span><span class="fn-arg">traitsImputed — complete trait matrix</span><span class="fn-arg">traitsMissing — species with missing traits</span></div>
```

```{r fn_pca_make_PCA_groups_1, eval=FALSE}
make.PCA.groups <- function(traitsImputed, traitsMissing,
                            group = c("Plants","Birds","Mammals","Reptiles","Amphibians","Fishes")) {
  require(paran)
  paranAux <- paran(traitsImputed[, 1:(ncol(traitsImputed) - 1)])
  if (paranAux$Retained < 2) paranAux$Retained <- 2
  dimensionsAux <- paranAux$Retained

  PCAImpute <- list()
  PCAImpute$traits <- traitsImputed[, 1:(ncol(traitsImputed) - 1)]
  PCAImpute$traitsMissing <- traitsMissing[, 1:(ncol(traitsImputed) - 1)]
  PCAImpute$nImputed <- rowSums(is.na(PCAImpute$traitsMissing))
  PCAImpute$PCA <- princomp(traitsImputed[, 1:(ncol(traitsImputed) - 1)])
  PCAImpute$dimensions <- dimensionsAux
  PCAImpute$variance <- (summary(PCAImpute$PCA)[1][[1]]^2)[1:dimensionsAux] /
    sum(summary(PCAImpute$PCA)[1][[1]]^2)

  if (group %in% c("Birds","Mammals")) { # Mammals & Birds
    multPC1 <- ifelse(PCAImpute$PCA$loadings["bm", "Comp.1"] > 0, 1, -1)
    PCAImpute$PCA$scores[, 1] <- multPC1 * PCAImpute$PCA$scores[, 1]
    PCAImpute$PCA$loadings[, "Comp.1"] <- multPC1 * PCAImpute$PCA$loadings[, "Comp.1"]
  }

  if (group == "Reptiles") { # Reptiles
    multPC2 <- ifelse(PCAImpute$PCA$loadings["inc", "Comp.2"] > 0, -1, 1)
    PCAImpute$PCA$scores[, 2] <- multPC2 * PCAImpute$PCA$scores[, 2]
    PCAImpute$PCA$loadings[, "Comp.2"] <- multPC2 * PCAImpute$PCA$loadings[, "Comp.2"]
  }

  if (group == "Fishes") { # Fishes
    multPC1 <- ifelse(PCAImpute$PCA$loadings["svl", "Comp.1"] > 0, 1, -1)
    multPC2 <- ifelse(PCAImpute$PCA$loadings["svl", "Comp.2"] < 0, 1, -1)
    PCAImpute$PCA$scores[, 1] <- multPC1 * PCAImpute$PCA$scores[, 1]
    PCAImpute$PCA$loadings[, "Comp.1"] <- multPC1 * PCAImpute$PCA$loadings[, "Comp.1"]
    PCAImpute$PCA$scores[, 2] <- multPC2 * PCAImpute$PCA$scores[, 2]
    PCAImpute$PCA$loadings[, "Comp.2"] <- multPC2 * PCAImpute$PCA$loadings[, "Comp.2"]
  }

  PCAImpute$loadings <- PCAImpute$PCA$loadings
  PCAImpute$traitsUse <- data.frame(PCAImpute$PCA$scores[, 1:PCAImpute$dimensions])

  treathCat <- c("EX", "EW", "CR", "EN", "VU")
  noThreatCat <- c("LC", "NT")
  PCAImpute$Threat <- with(traitsImputed,
                           ifelse(IUCN %in% treathCat, 1,
                                  ifelse(IUCN %in% noThreatCat, 0, NA)))
  return(PCAImpute)
}
```

```{=html}
</div>
```

```{=html}
<h3 class="fn-section-divider"><span>Visualisation</span><hr></h3>
```

```{=html}
<div class="fn-card">
<div class="fn-header">
  <div>
    <h4 class="fn-name" id="fn-pca-plot">PCA_plot(PCoAPlot, PCoACorPlot, multAx1, multAx, title, legend)</h4>
    <div class="fn-desc">Biplot of PCA scores with correlation arrows and optional group legend.</div>
    <div class="fn-tags"><span class="fn-tag tag-viz">visualisation</span><span class="fn-tag tag-fd">functional diversity</span></div>
  </div>
</div>
<div class="fn-args"><span class="fn-args-label">Args:</span><span class="fn-arg">PCoAPlot — score data frame</span><span class="fn-arg">PCoACorPlot — loading data frame</span><span class="fn-arg">multAx1/multAx — axis scaling</span><span class="fn-arg">title — plot title</span><span class="fn-arg">legend — show legend</span></div>
```

```{r fn_pca_PCA_plot_2, eval=FALSE}
PCA_plot <- function(PCoAPlot,PCoACorPlot,multAx1,multAx,title,legend){
  Plan12=ggplot(data = data.frame(PCoAPlot$vectors)) +
    geom_point(aes(x = Axis.1, y = Axis.2),col="grey89") +
    theme_classic()+
    ggtitle(title)+
    xlab(paste0("PCoA 1 (",round(PCoAPlot$values[1,2]*100,2),"%)"))+
    ylab(paste0("PCoA 2 (",round(PCoAPlot$values[2,2]*100,2),"%)"))+
    #coord_fixed() + ## need aspect ratio of 1!
    geom_segment(data = data.frame(PCoACorPlot),
                 aes(x = 0, xend = Axis.1*multAx, y = 0, yend = Axis.2*multAx),
                 arrow = arrow(length = unit(0.25, "cm")), colour = PCoACorPlot$color) +
    geom_text(data = data.frame(PCoACorPlot), 
              aes(x =  Axis.1*multAx1, y = Axis.2*multAx1, 
                  label =  names), colour = PCoACorPlot$color,
              size = 5) +
    theme(
      plot.title = element_text(
        size = 18,       # Increase size (adjust as needed)
        face = "bold"    # Make it bold
      )
    )
  
  if(!is.null(legend)){
    Plan12 = Plan12 + annotate(geom = "text",label = legend, x = Inf, y = -Inf, hjust = 1, vjust = -0.5)      # Make text bold)
  }
  
  
  
  Plan12
}
```

```{=html}
</div>
```

```{=html}
<div class="fn-card">
<div class="fn-header">
  <div>
    <h4 class="fn-name" id="fn-pca-plot-funspace">PCA_plot_funspace(PCoAPlot, PCoACorPlot, ...)</h4>
    <div class="fn-desc">Functional-space biplot with KDE density shading and phylopic image overlays.</div>
    <div class="fn-tags"><span class="fn-tag tag-viz">visualisation</span><span class="fn-tag tag-fd">functional diversity</span></div>
  </div>
</div>
<div class="fn-args"><span class="fn-args-label">Args:</span><span class="fn-arg">PCoAPlot — score data frame</span><span class="fn-arg">PCoACorPlot — loading data frame</span><span class="fn-arg">legend — optional legend</span></div>
```

```{r fn_pca_PCA_plot_funspace_3, eval=FALSE}
PCA_plot_funspace <- function(PCoAPlot, PCoACorPlot, multAx1, multAx, title, legend = NULL,
                              probs = c(0.25, 0.5, 0.99),
```

```{=html}
</div>
```

```{=html}
<div class="fn-card">
<div class="fn-header">
  <div>
    <h4 class="fn-name" id="fn-make-pca-plot">make_pca_plot(pca_object, correlation_data, ...)</h4>
    <div class="fn-desc">PCA correlation plot with 2D density legend (axes 1–2).</div>
    <div class="fn-tags"><span class="fn-tag tag-viz">visualisation</span></div>
  </div>
</div>
<div class="fn-args"><span class="fn-args-label">Args:</span><span class="fn-arg">pca_object — PCA result</span><span class="fn-arg">correlation_data — loadings</span><span class="fn-arg">multAx1/multAx — scaling</span><span class="fn-arg">title — plot title</span></div>
```

```{r fn_pca_make_pca_plot_4, eval=FALSE}
make_pca_plot <- function(pca_object, correlation_data, multAx1, multAx, title) {
  legend_text <- paste0(
    "Space occupied by:\n",
    "50% = ", round(pca_object$ALLDensity[1, "0.5"], 2), "\n",
    "99% = ", round(pca_object$ALLDensity[1, "0.99"], 2)
  )
  PCA_plot(pca_object$PCoA, correlation_data, multAx1, multAx, title, legend_text)
}
```

```{=html}
</div>
```

```{=html}
<div class="fn-card">
<div class="fn-header">
  <div>
    <h4 class="fn-name" id="fn-make-pca-plot-34">make_pca_plot_34(pca_object, correlation_data, ...)</h4>
    <div class="fn-desc">Same as make_pca_plot but for axes 3–4.</div>
    <div class="fn-tags"><span class="fn-tag tag-viz">visualisation</span></div>
  </div>
</div>
<div class="fn-args"><span class="fn-args-label">Args:</span><span class="fn-arg">pca_object — PCA result</span><span class="fn-arg">correlation_data — loadings</span></div>
```

```{r fn_pca_make_pca_plot_34_5, eval=FALSE}
make_pca_plot_34 <- function(pca_object, correlation_data, multAx1, multAx, title) {
  legend_text <- paste0(
    "Space occupied by:\n",
    "50% = ", round(pca_object$ALLDensity[1, "0.5"], 2), "\n",
    "99% = ", round(pca_object$ALLDensity[1, "0.99"], 2)
  )
  PCA_plot_34(pca_object$PCoA, correlation_data, multAx1, multAx, title, legend_text)
}
```

```{=html}
</div>
```

```{=html}
<h3 class="fn-section-divider"><span>Tables & Procrustes</span><hr></h3>
```

```{=html}
<div class="fn-card">
<div class="fn-header">
  <div>
    <h4 class="fn-name" id="fn-format-correlation-table">format_correlation_table(PCoACorPlot, shortNames)</h4>
    <div class="fn-desc">Format a PCA loading table with colour-coded cells and abbreviated trait names.</div>
    <div class="fn-tags"><span class="fn-tag tag-util">utility</span><span class="fn-tag tag-viz">visualisation</span></div>
  </div>
</div>
<div class="fn-args"><span class="fn-args-label">Args:</span><span class="fn-arg">PCoACorPlot — loading data frame</span><span class="fn-arg">shortNames — abbreviated trait labels</span></div>
```

```{r fn_pca_format_correlation_table_6, eval=FALSE}
format_correlation_table <- function(PCoACorPlot, shortNames) {
  cbind.data.frame(
    PCoACorPlot,
    color = shortNames[match(rownames(PCoACorPlot), shortNames$original), "color"],
    names = shortNames[match(rownames(PCoACorPlot), shortNames$original), "short"]
  )
}
```

```{=html}
</div>
```

```{=html}
<div class="fn-card">
<div class="fn-header">
  <div>
    <h4 class="fn-name" id="fn-run-procrustes">run_procrustes(PCA_list)</h4>
    <div class="fn-desc">Procrustes analysis comparing PCA spaces across multiple groups or datasets.</div>
    <div class="fn-tags"><span class="fn-tag tag-stat">statistics</span><span class="fn-tag tag-fd">functional diversity</span></div>
  </div>
</div>
<div class="fn-args"><span class="fn-args-label">Args:</span><span class="fn-arg">PCA_list — named list of PCA score matrices</span></div>
```

```{r fn_pca_run_procrustes_7, eval=FALSE}
run_procrustes <- function(PCA_list) {
  combNames <- combn(names(PCA_list), 2)
  combNames <- apply(combNames, 2, function(x) paste0(x[1], "_", x[2]))
  procrustes_table <- matrix(NA, ncol = 1, nrow = length(combNames),
                             dimnames = list(combNames, 'Birds'))
  
  for (j in 1:length(PCA_list)) {
    x <- PCA_list[[j]]$PCoA$vectors
    for (i in 1:length(PCA_list)) {
      if (i > j) {
        y <- PCA_list[[i]]$PCoA$vectors
        rownames(y) = rownames(x)
        prcTest <- ade4::procuste.rtest(as.data.frame(x), as.data.frame(y), nrepet = 999)
        cor_coef <- prcTest$obs
        p_val <- prcTest$pvalue
        signif <- ifelse(p_val < 0.001, "***", ifelse(p_val < 0.01, "**", ifelse(p_val < 0.05, "*", "ns")))
        procrustes_table[paste0(names(PCA_list)[j], "_", names(PCA_list)[i]), 1] <- 
          paste0(round(cor_coef, 3), " ", signif)
      }
    }
  }
  return(procrustes_table)
}
```

```{=html}
</div>
```
